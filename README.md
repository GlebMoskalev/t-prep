# T-Prep Backend

Backend API для приложения T-Prep - системы подготовки к экзаменам с использованием методики интервальных повторений.

## 🎯 Возможности

- 🔐 Авторизация через Google OAuth 2.0 (OIDC)
- 🎫 JWT токены для аутентификации
- 📚 Управление модулями и карточками
- 🧠 **Система интервальных повторений (SFRS-подобная)**
  - Адаптивные интервалы повторения
  - Отслеживание прогресса (Learning → Review → Relearning)
  - Динамические параметры сложности и стабильности
  - Экспоненциальный рост интервалов
- 🐳 Docker контейнеризация
- 📊 PostgreSQL база данных с Alembic миграциями
- ⚡ FastAPI + SQLAlchemy + Pydantic

## Архитектура

```
app/
├── api/                    # API endpoints
│   ├── v1/                # API версии 1
│   └── endpoints/         # Отдельные endpoints
├── core/                  # Основная конфигурация
│   ├── config.py         # Настройки приложения
│   ├── security.py       # JWT и безопасность
│   └── deps.py           # Зависимости
├── db/                    # База данных
│   └── database.py       # Подключение к БД
├── models/                # SQLAlchemy модели
│   ├── user.py           # Модель пользователя
│   ├── module.py         # Модель модуля
│   ├── card.py           # Модель карточки
│   ├── interval_repetition.py  # Интервальные повторения
│   └── module_access.py  # Права доступа
├── schemas/               # Pydantic схемы
├── services/              # Бизнес-логика
│   ├── auth_service.py   # Сервис авторизации
│   ├── google_oauth_service.py  # Google OAuth
│   └── repetition_service.py    # Интервальные повторения
└── utils/                 # Утилиты
```

## Установка и запуск

### Локальная разработка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd t-prep
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе `env.example`:
```bash
cp env.example .env
```

5. Настройте переменные окружения в `.env`:
```env
DATABASE_URL=postgresql://tprep_user:tprep_password@localhost:5432/tprep_db
SECRET_KEY=your-secret-key-here
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
```

6. Запустите приложение:
```bash
uvicorn main:app --reload
```

### Docker

1. Запустите все сервисы:
```bash
docker-compose up -d
```

2. Приложение будет доступно по адресу: http://localhost:8000

## API Endpoints

### Авторизация
- `GET /api/v1/auth/google` - Получить URL для Google OAuth
- `POST /api/v1/auth/google/callback` - Обработка callback от Google
- `GET /api/v1/auth/me` - Получить информацию о текущем пользователе
- `POST /api/v1/auth/logout` - Выход из системы

### Модули
- `GET /api/v1/modules/` - Получить все модули пользователя
- `POST /api/v1/modules/` - Создать новый модуль
- `GET /api/v1/modules/{module_id}` - Получить модуль по ID
- `PUT /api/v1/modules/{module_id}` - Обновить модуль
- `DELETE /api/v1/modules/{module_id}` - Удалить модуль

### Карточки
- `GET /api/v1/cards/module/{module_id}` - Получить карточки модуля
- `POST /api/v1/cards/` - Создать новую карточку
- `GET /api/v1/cards/{card_id}` - Получить карточку по ID
- `PUT /api/v1/cards/{card_id}` - Обновить карточку
- `DELETE /api/v1/cards/{card_id}` - Удалить карточку

### 🧠 Интервальные повторения
- `POST /api/v1/repetitions/card/{card_id}/start` - Начать повторение карточки
- `POST /api/v1/repetitions/card/{card_id}/answer` - Отправить ответ на карточку
  ```json
  { "is_correct": true }
  ```
- `GET /api/v1/repetitions/due?hours_ahead=12` - Получить карточки для повторения
- `GET /api/v1/repetitions/overdue` - Получить просроченные карточки
- `GET /api/v1/repetitions/statistics` - Получить статистику пользователя
- `POST /api/v1/repetitions/module/{module_id}/initialize` - Инициализировать повторения для модуля
- `GET /api/v1/repetitions/card/{card_id}` - Получить информацию о повторении карточки

### Пользователи
- `GET /api/v1/users/me` - Получить профиль пользователя
- `PUT /api/v1/users/me` - Обновить профиль пользователя

## Настройка Google OAuth

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google+ API
4. Создайте OAuth 2.0 credentials
5. Добавьте redirect URI: `http://localhost:8000/api/v1/auth/google/callback`
6. Скопируйте Client ID и Client Secret в файл `.env`

## Структура базы данных

### Таблицы:
- `users` - Пользователи системы (с OIDC идентификатором)
- `modules` - Модули (наборы карточек)
- `cards` - Карточки для изучения
- `interval_repetitions` - Интервальные повторения (ключевая таблица)
- `module_accesses` - Права доступа к модулям

### Модель IntervalRepetition:
```python
{
  "card_id": int,              # ID карточки
  "user_id": int,              # ID пользователя
  "state": str,                # Learning / Review / Relearning
  "step": int,                 # Текущий шаг (0, 1, 2, ...)
  "stability": float,          # Стабильность знания (0.1 - 10.0)
  "difficulty": float,         # Сложность карточки (0.1 - 1.0)
  "due": datetime,             # Время следующего повторения
  "last_review": datetime      # Время последнего повторения
}
```

## 🧠 Алгоритм интервальных повторений

### Начальные значения (при первом создании):
- `state = Learning`
- `step = 0`
- `stability = 0.5`
- `difficulty = 0.3`
- `due = now + 20 минут`

### При правильном ответе ✅:
1. **Увеличить stability**: `stability * 1.3 + 0.1` (макс 10.0)
2. **Уменьшить difficulty**: `difficulty - 0.05` (мин 0.1)
3. **Увеличить step**: `step + 1`
4. **Пересчитать due** (экспоненциальный рост):
   - step 0-1: +8 часов
   - step 2: +24 часа (1 день)
   - step 3: +72 часа (3 дня)
   - step 4+: экспоненциальный рост `72 * 2^(step-3)`
5. **Перейти в Review** после 3+ правильных ответов подряд

### При неправильном ответе ❌:
1. **Уменьшить stability**: `stability * 0.5` (мин 0.1)
2. **Увеличить difficulty**: `difficulty + 0.1` (макс 1.0)
3. **Сбросить step**: `step = 0`
4. **Установить due**: `now + 10 минут`
5. **Перейти в Relearning**

### Пример работы:
```
Карточка создана → Learning (due: +20 мин)
  ↓ Правильный ответ
Learning (step: 1, due: +8 часов, stability: ↑)
  ↓ Правильный ответ
Learning (step: 2, due: +1 день, stability: ↑↑)
  ↓ Правильный ответ
Review (step: 3, due: +3 дня, stability: ↑↑↑)
  ↓ Неправильный ответ
Relearning (step: 0, due: +10 мин, stability: ↓)
```

### Получение карточек для повторения:
```python
# Получить карточки, которые нужно повторить в ближайшие 12 часов
GET /api/v1/repetitions/due?hours_ahead=12

# Получить просроченные карточки (due < now)
GET /api/v1/repetitions/overdue
```

## Разработка

### Добавление новых endpoints:
1. Создайте схему в `app/schemas/`
2. Добавьте endpoint в `app/api/endpoints/`
3. Зарегистрируйте в `app/api/v1/api.py`

### Миграции базы данных:
```bash
# Создать миграцию
alembic revision --autogenerate -m "Description"

# Применить миграции
alembic upgrade head

# В Docker:
docker compose exec web alembic upgrade head
```

## 📝 Примеры использования API

### 1. Авторизация
```bash
# Получить URL для Google OAuth
curl http://localhost/api/v1/auth/google

# После авторизации получить информацию о пользователе
curl -H "Authorization: Bearer YOUR_TOKEN" \
  http://localhost/api/v1/auth/me
```

### 2. Работа с модулями и карточками
```bash
# Создать модуль
curl -X POST http://localhost/api/v1/modules/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name":"Python","description":"Изучение Python"}'

# Создать карточку
curl -X POST http://localhost/api/v1/cards/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "module_id": 1,
    "question": "Что такое list comprehension?",
    "answer": "Синтаксический способ создания списков"
  }'
```

### 3. Интервальные повторения
```bash
# Инициализировать повторения для модуля
curl -X POST http://localhost/api/v1/repetitions/module/1/initialize \
  -H "Authorization: Bearer YOUR_TOKEN"

# Получить карточки для повторения
curl -H "Authorization: Bearer YOUR_TOKEN" \
  "http://localhost/api/v1/repetitions/due?hours_ahead=12"

# Ответить на карточку (правильно)
curl -X POST http://localhost/api/v1/repetitions/card/1/answer \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"is_correct": true}'

# Получить статистику
curl -H "Authorization: Bearer YOUR_TOKEN" \
  http://localhost/api/v1/repetitions/statistics
```

### Пример ответа статистики:
```json
{
  "total_cards": 10,
  "learning": 3,
  "review": 5,
  "relearning": 2,
  "due_today": 4,
  "overdue": 1
}
```
